import React from 'react'

const Button = ({title}) => {
    return (
        <div>
            <button className='bg-[tomato] rounded-[10px] text-[18px]  px-3 py-1  '>{title} </button>

        </div>
    )
}

export default Button
