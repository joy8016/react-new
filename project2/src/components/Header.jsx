import React from 'react'

const Header = ({Data}) => {

    console.log(Data);
  return (
    <div style={{background:'red', height:'10vh'}}>
        header
      
    </div>
  )
}

export default Header
